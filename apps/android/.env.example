# SecureStream Android local defaults.
# The current Android app keeps the API URL in SecureStreamApi.kt.
# This file exists so clean Gradle builds work with the secrets plugin.
SECURESTREAM_API_URL=https://compreface.3dbpoint.com/api/v1/
